{
  "name": "winmigrate",
  "version": "0.1.0",
  "description": "Portable Windows environment backup/restore with path remapping",
  "type": "module",
  "private": true,
  "bin": { "winmigrate": "./src/cli.ts" },
  "scripts": {
    "start": "bun run src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
